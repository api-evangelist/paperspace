name: paperspace-models-models
description: |
  Naftiko capability for the Paperspace Gradient model registry.
api: paperspace-models-api
operations:
- listModels
- createModel
- getModel
- deleteModel
auth:
  type: bearer
  header: Authorization
  prefix: Bearer
  env: PAPERSPACE_API_TOKEN
